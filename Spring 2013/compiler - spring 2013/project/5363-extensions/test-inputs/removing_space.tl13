%- removing space to allow adjacent identifer/numbers/keyword -%

program 
  var N as int; 
  var SQRT as int; 
begin 
  N:=readInt; 
  SQRT:=0; 

  while SQRT*SQRT<=N do % works with without space 
    SQRT:=SQRT+1; 

  end; 

  SQRT:=(SQRT-1)+10 ; 

  writeInt SQRT ; 

end